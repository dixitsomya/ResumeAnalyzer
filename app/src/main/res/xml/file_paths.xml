<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Downloads folder ko share karne ke liye -->
    <external-path
        name="downloads"
        path="Download/" />

    <!-- Agar app-specific folder use karna ho to -->
    <external-files-path
        name="my_files"
        path="." />

    <!-- Cache directory -->
    <cache-path
        name="cache"
        path="/" />

</paths>